# AUDIT_LEDGER — HARAN/JEFF/MR.JEFFREY full-file audit (v1 directive)

Resume rule: pick up from the first `.py` file (in the batch order defined by the directive §2) that does
not yet have a row below. `verdict ∈ {CLEAN, FIXED, FLAG, N/A}`. Every row is backed by grep hits / test
results / line references — no un-evidenced "looks fine".

## Batch 0 — setup

| item | result |
|---|---|
| baseline `test_build.py` (pinned single-thread env) | 276 passed / 276 (matches STATUS.md) |
| baseline `test_catalog.py` (pinned single-thread env) | 250 passed, **1 failed** (`test_bq_metakernel`) — see row below; this was a timing artifact of editing `kernel_verdict.py` while the baseline ran in background, not a pre-existing repo defect (re-confirmed clean before any edit by inspecting `git status` at session start) |
| Pass 1 triage: A1 emission-bypass (`grade\|status\|verdict`: EXACT literal) | 8 file hits (directive estimated ~37) → 6 real candidates after excluding `test_catalog.py` (test, out of file-audit scope) and `projectharan_all_code.py` (stale bundle artifact, not source) |
| Pass 1 triage: A4 assert-near-soundness-word | 1288 hits (directive estimated ~751; repo has grown) — Pass 2 classifies per-file, default CLEAN |
| Pass 1 triage: A6 sampling-near-EXACT | 198 hits (directive estimated ~148) — Pass 2 classifies per-file |
| Pass 1 triage: B4 forbidden words | 24 hits (directive estimated ~25) — Pass 2 classifies (rule-statement vs claim) |
| `dependency_audit.py` → `final_dependency_set()["forbidden_present"]` | `[]` — clean, C1 safety rail holds |
| `catalog/arsenal_report.py`, `test_moat_battery`, `*_dogfood` | confirmed present, reused (not rebuilt) |

## Batch 1 — highest risk (A1 hits + named root disposers + `*_grade` files)

| # | file | pkg | role | verdict | issue(class) | fix + regression | commit |
|---|------|-----|------|---------|--------------|------------------|--------|
| 1 | `kernel_verdict.py` | root | disposer (the ADT itself) | FIXED | §7 first task (not a violation — the prescribed emission-boundary extension) | added `Verdict.as_dict()` (JSON-safe projection, "grade" key not aliased as "status" to avoid collision with bespoke response shapes) + module-level `to_api()` (constructs a real `Verdict` internally, so `__post_init__`'s soundness gates apply at the emission boundary; `extras` cannot override enforced core fields — the smuggling defense). Regression: `test_br1_emission_gate_survives_dash_O` (test_build.py) — in-process rubber-stamp rejection, `-O` subprocess proving the gate survives assert-stripping, smuggling-defense assertion. **Collateral fix**: `test_catalog.py`'s `test_bq_metakernel` had `kernel_verdict.py` in its "0-diff protected files" list from §BQ's original construction-time check; since this ADT is meant to be extended additively over time (this being the first case), removed it from that list with an explanatory comment (its job was "did §BQ's OWN commit touch this", never "freeze forever") — verified `test_bq_metakernel` still passes (all its OWN battery/cross-check assertions unaffected, only the stale protected-list entry removed). | pending |
| 2 | `algo50_router.py` | root | disposer | FIXED | A1 emission bypass — `route()`'s BROTH_HIT branch returned a raw `{"grade": "EXACT", ...}` dict literal | Routed through `kernel_verdict.to_api()` per the directive's own worked example. `passed=True` justified by measurement: `test_build.py:10091` (`test_...` broth battery, part of the mandatory gate) calls `HB.reverify()` on a SAMPLE of broth entries every commit, including a tampered-entry negative control (line ~10112) — so the "pre-proven, re-checkable" claim is gate-enforced, not a rubber stamp; `route()` itself does NOT call `reverify()` per-lookup (would defeat the O(1) purpose), by design. Regression: existing `test_haran_tier_routing` (test_build.py) re-verified standalone — passes unchanged (it checks `h["grade"] == KV.EXACT`, `h["action"]`, `h["ui"]`, `hb["value"]`, all preserved as `to_api()` extras). | pending |
| 3 | `webapi/engine_bridge.py` | webapi | disposer (the live product-facing emission boundary) | FIXED | A1 emission bypass — `_loop_collapse()`'s "nested_sum" and "code_shape" branches each hardcoded `"grade": "EXACT"` while carrying `nd.certificate`/`sd.certificate` as bare descriptive STRINGS (not `KV.Cert` objects) — the highest-risk file in this batch since it is literally the live per-request loop-collapse API | For both sites: routed through `kernel_verdict.to_api()`, wrapping the existing descriptive-string certificate as `Cert(..., passed=True, detail=<string>)`. `passed=True` justified by measurement: read BOTH `structure_recognizer._offload_nested` (line 854) and `_offload_closed_form` (line 778) in full — each has its OWN in-function differential-equivalence gate (`if checked < 5 or ok != checked: return Dispatch(NONE, ...)`) that MUST pass before `Dispatch("OFFLOADED", ...)` is ever returned, so `status=="OFFLOADED"` is never speculative. `Dispatch.certificate: str` (structure_recognizer.py line 84) confirmed to be a plain descriptive string, not an ADT object — preserved verbatim as `Cert.detail`. Checked `web/src` frontend for any `.certificate`-shape consumer (none found — safe to change certificate's JSON shape from string→dict) and Python callers (`test_build.py`/`loop_collapse_bench.py` — only truthiness checks on `certificate`, never string ops). Regression: 5 existing tests re-verified standalone (`test_haran_nested_loop_collapse`, `test_haran_filtered_loop_collapse`, `test_loop_collapse_adversarial`, `test_loop_collapse_coverage`, `test_loop_collapse_fork_safe`) + `test_bk_production_wiring_and_pipeline_fold` (test_catalog.py) — all pass unchanged; additionally hand-verified both fix sites directly with fresh nested-sum and while-loop inputs, confirming `grade=="EXACT"` + `isinstance(certificate, dict)` + `certificate["passed"] is True`. | pending |
| 4 | `barrierfold/barrierfold_report.py` | barrierfold | report | CLEAN | A1 grep hit (7 static `"grade": "EXACT"` entries in a `report() -> dict` islands table, e.g. I2–I6) | Confirmed via code read: `report()` takes NO parameters — it is a fixed, hand-authored descriptive table summarizing the ARCHITECTURE of already-built/already-tested islands (each island's OWN engine carries its OWN real certificate elsewhere); this is documentation-as-data, never a live per-instance disposer emission. Per §3 role table ("report/measure/UI/panel... grade 문자열은 대개 표시 → 대개 CLEAN"). Only external consumer: `test_catalog.py` (structural/battery-roll-up check). | — |
| 5 | `extract/semiring_lens.py` | extract | organizing/taxonomy (not a disposer) | CLEAN | A1 grep hit (`_SEMIRINGS` registry: 4 static `"grade": "EXACT"` entries + `classify()` looks the grade up by semiring-family keyword match) | Module's own docstring: "an ORGANIZING frame, not a build item... a unifying VIEW... NOT a new mechanism". `classify(reduction)` keyword-matches a loop's reduction-operator TEXT to a semiring FAMILY (count/cost/reach/parity/probability) and returns that family's well-known mathematical grade as a LABEL — it is not certifying any specific computation's numeric output. **Measured blast radius: 0 external callers besides `aq_report.py`'s own `adversarial_battery()["all_ok"]` roll-up** (`grep -rn "semiring_lens\|SemiringView"` — only hit outside its own file and the stale bundle). Nothing in the live dispatch path (`webapi/engine_dispatch.py`) reaches this. If ever wired live to grade a SPECIFIC user computation from this label alone, that would become a genuine A3 violation — noted here for future reference, not treated as one today given zero measured reachability. | — |
| 6 | `foldaxes/foldaxes_report.py` | foldaxes | report | CLEAN | A1 grep hit (`"grand_decomposition"` static table entry `"EXACT": {"grade": "EXACT", ...}`) | Same shape as #4: `report() -> dict` takes no parameters, a fixed descriptive breakdown of the engine's own grade taxonomy for a human-readable report, not a live emission. | — |
| 7 | `recall_integrate.py` | root | disposer-shaped, but currently unreachable from any live path | **FLAG** | A3 grade-by-name-set — `compose_lenses(structure)`'s `known = {"gf","window","mobius","modular","galois","prony"}`; `both_known = all(leg in known ...)` ⇒ `"grade": "EXACT" if both_known else "DECLINE"`. This is a near-literal match of the directive's OWN cited example (`known={...,"prony"}` name-set grading) — no certificate is checked for either "leg", only that both leg NAMES are members of a fixed set. | **Not fixed — flagged for owner review.** Measured: `grep`-confirmed zero live callers — `compose_lenses` is exercised ONLY by its own `adversarial_battery()` and a matching `test_catalog.py:4628` assertion, both with FIXED, hand-chosen inputs (`"gf×window"`, `"gf×nope"`); not reachable from `webapi/engine_dispatch.py` or any request-facing path; `engine_inventory.py`/`upgrade_ah_report.py` list it only for gap-tracking/battery-rollup purposes, not live dispatch. So TODAY'S blast radius is genuinely zero. However, the module's own docstring says it "models the disposition" (self-aware placeholder, not a real certificate check) for what the file's own header claims is "raise the RECALL... of the already-saturated 22 mechanisms" — closing this gap properly would mean actually checking each named lens's OWN certificate against the real input, which is materially new infrastructure, not a 1-file minimal fix, and risks over-editing (Prime Directive §2: don't force a fix without full confidence). Recommend: either (a) leave as an explicitly-scoped internal model (rename the "grade" key to something non-ADT-colliding like "would_grade" to remove the vocabulary collision without touching behavior), or (b) if/when this is ever wired to a live path, build the real per-leg certificate check first. Left un-touched pending owner direction. | — |
| 8 | `projectharan_all_code.py` | root | generated bundle artifact (not source) | N/A | A1 grep hit — but this file is a machine-generated concatenation of the OTHER files (`bundle_all_code.py`'s output), already stale (predates qmkernel/metakernel). Not part of the 709 non-test engine files in any meaningful sense — editing it would be immediately overwritten by the next regeneration and duplicates fixes already made at the real source locations. | Out of audit scope — noted, not touched. | — |
| 9 | `test_catalog.py` | root | test (role: skip unless tautological/wrong-grade-assert) | N/A (not part of the 709-file audit; touched only as a direct collateral consequence of row #1) | A1 grep hit is the test file's OWN assertions checking real engine output (`assert C.route(...).grade == KV.EXACT` etc.) — not a violation, this is what a test is supposed to do | One line changed as documented in row #1 (removed `kernel_verdict.py` from `test_bq_metakernel`'s protected-file list). | pending |

| 10 | `structure_recognizer.py` | root | disposer/recognizer (offload gates) | CLEAN | named root disposer | `Dispatch.certificate` is a plain descriptive string (not KV-ADT — this file predates/doesn't use `kernel_verdict`, has its own `Dispatch` type). Read `_offload_nested` (854) and `_offload_closed_form` (778) in full for row #3's fix: BOTH gate on `checked>=5 and ok==checked` differential-equivalence BEFORE ever returning `Dispatch("OFFLOADED", ...)` — no speculative OFFLOADED status exists. `dispatch()` (1066) and `decide_loop` (1100) route only into these already-gated paths or return `NONE`/`None` honestly. No raw `"EXACT"` literal anywhere in the file (grep-confirmed). | — |
| 11 | `cfinite.py` | root | verifier (self-certifying) | CLEAN | named root disposer | 0 `kernel_verdict` usage (pre-ADT / self-contained file) — `verify_cfinite` independently re-derives via the companion-power path AND compares against the naive path on probe points (matches METAUPGRADE_INDEX.md's prior, already-measured finding from §BQ: "already a self-certifying algorithm"). No raw grade literals. | — |
| 12 | `freivalds.py` | root | verifier (probabilistic, honest) | CLEAN | named root disposer | 0 `kernel_verdict` usage — one-sided-error certifying check (Freivalds δ=2⁻ᵏ), always graded PROBABILISTIC via its own `CertResult`, never EXACT (matches METAUPGRADE_INDEX.md §2's prior finding). No raw grade literals. | — |
| 13 | `chc_solve.py` | root | disposer (CHC safety) | CLEAN | named root disposer | Read `chc_grade()` (line 77-90) and `prove_safety_chc`'s re-verification (lines 60-74) in full: THREE fresh `z3.Solver()` instances independently re-check `init⇒Inv`, `Inv∧trans⇒Inv'`, `Inv⇒prop` before EXACT is ever emitted (lines 64/67/68) — SAFE and UNSAFE are BOTH genuine EXACT-graded outcomes (grading certainty-of-the-answer, not optimism), UNKNOWN/error → honest DECLINE. Matches METAUPGRADE_INDEX.md's prior finding exactly (same line numbers). Already the template §BQ's NEW-1 generalized — confirmed still 0-diff-worthy, unmodified. | — |
| 14 | `ic3_pdr.py` | root | verifier (k-induction) | CLEAN | named root disposer | 0 `kernel_verdict` usage — implements k-induction (base+induction case both z3-proved before SAFE), UNSAFE only with a concrete counterexample, else UNKNOWN; "never false SAFE" was already the discipline pre-audit (matches METAUPGRADE_INDEX.md §3's prior finding, same file). No raw grade literals. | — |
| 15 | `fast_certificates.py` | root | verifier (certifying, honest) | CLEAN | named root disposer | 0 `kernel_verdict` usage — Freivalds/Schwartz-Zippel style certifying checks, one-sided error, always PROBABILISTIC (matches METAUPGRADE_INDEX.md §2's prior finding). No raw grade literals. | — |
| 16 | `algo50.py` | root | registry (the 50-algorithm catalog + `*_grade`-shaped entries) | CLEAN | 49 `KV.*` usages — the file with the most ADT surface area in this slice | Grep-confirmed 0 raw `grade =` assignments outside `KV.*` constructors — every grade in this 309-line registry is produced through the ADT, not a hand-written literal. (Full per-entry precondition audit of all 50 algorithm certificate paths deferred to a dedicated pass — this row certifies the emission-boundary/A1 class only; a deeper A2/A9 precondition read of `algo50.py`'s individual algorithm implementations is queued for a future batch, noted here rather than silently skipped.) | — |

## Batch 1 continued — remaining `*_grade`-function files (93 files, precisely re-grepped this session —
`grep -lE "def [A-Za-z_]*_grade[A-Za-z_]*\("`, superseding the directive's rough ~115 estimate)

Method: six parallel read-only research passes (one per directory group below) located every `_grade`-named
function, read it plus anything it directly calls, and reported the specific verification step (or its absence)
with line numbers — same bar as rows 1–16 above ("backed by grep hits / test results / line references"). Every
finding was then spot-checked directly against the source by the auditor (not accepted at face value): 5 files
across all 6 groups (`mathmode/number_theory.py`, `pillar3/termination.py`, `catalog/mech_kregular.py`,
`native_modelcount.py`, `gpu/ptx_codegen.py`) were independently re-read and the cited line numbers/verification
logic confirmed accurate before any row below was written. Verdict for all 93: **CLEAN** — every disposer
performs a real in-function (or same-file-called) verification — z3 proof, differential/cross-check against an
independent method, exact re-substitution, exhaustive enumeration with a stated bound, or `kernel_verdict.KV.*`
ADT construction — before an EXACT/PROBABILISTIC grade is emitted, and every one has a genuine DECLINE fallback.
Zero instances of the `recall_integrate.py`-style name-set-membership shortcut or a raw ungated literal.

### mathmode/ (16 files — MATH-mode solvers; certificate = an independent re-derivation/cross-check, not the
computation itself; heavy lifting (sympy/z3) is offloaded, the CHECK is always the arsenal's own)

| # | file | pkg | role | verdict | issue(class) | fix + regression | commit |
|---|------|-----|------|---------|--------------|------------------|--------|
| 17 | `mathmode/algebra.py` | mathmode | disposer | CLEAN | `*_grade` grep hit (6 fns: factor/poly_gcd/solve_poly/interpolate/partial_fractions/solve_system) | Every result re-verified by substitution: factorization multiplies back exactly (L30), gcd divides both inputs (L47), each root substitutes to 0 (L68), interpolant hits every point (L86), decomposition recombines (L102), every solution satisfies every equation (L128). | — |
| 18 | `mathmode/calculus.py` | mathmode | disposer | CLEAN | `*_grade` grep hit (4 fns) | `integrate_grade` symbolic diff-back (L52) + independent numeric quadrature cross-check (L22-37); `differentiate_grade` finite-difference cross-check at 4 points (L90-98); `taylor_grade` verifies Tᵏ(a)=fᵏ(a) for k=0..n (L119-122). | — |
| 19 | `mathmode/certified_numeric.py` | mathmode | disposer | CLEAN | `*_grade` grep hit (4 fns) | Sturm root count cross-checked against independent `sp.real_roots()` (L36); IVT sign-change + bisection-invariant preservation (L59-70); sqrt bracket `lo²≤n≤hi²` is itself the exact-rational certificate (L91); Monte Carlo π correctly graded PROBABILISTIC with stated Hoeffding δ, never EXACT (L111-113). | — |
| 20 | `mathmode/combinatorics.py` | mathmode | disposer | CLEAN | `*_grade` grep hit (4 fns) | Gosper telescoping identity re-checked + 24-point exact spot-check (L28-47,62); definite sum compared against brute-force exact sum (L82); Pascal's-rule and dual-closed-form cross-checks (L98, L111). | — |
| 21 | `mathmode/curvature.py` | mathmode | disposer | CLEAN | `*_grade` grep hit (2 fns) | Ricci tensor checked ≡0 for all 16 components (L105) + independent Kretschmann-scalar identity check (L110) before Schwarzschild EXACT. | — |
| 22 | `mathmode/differential.py` | mathmode | disposer | CLEAN | `*_grade` grep hit (1 fn) | `sp.checkodesol` substitution proof (L53) AND an independent numeric residual cross-check at 3 points (L19-34, L55) — both required. | — |
| 23 | `mathmode/geometry.py` | mathmode | disposer | CLEAN | `*_grade` grep hit (4 fns) | Every one uses TWO independent methods that must agree: shoelace vs fan-triangulation (L50), CCW-turn + containment (L79,81), on-both-segments exact collinearity (L99), ray-cast parity vs winding number (L135). | — |
| 24 | `mathmode/graph.py` | mathmode | disposer | CLEAN | `*_grade` grep hit (2 fns) | Dijkstra optimality certificate (source=0 + edge feasibility + tightness, L42-45); bipartite odd-cycle witness re-walked + 2-coloring re-checked monochromatic-free (L95,100). | — |
| 25 | `mathmode/inequalities.py` | mathmode | disposer | CLEAN | `*_grade` grep hit (2 fns) | Nonnegativity structural check cross-checked against an explicit negative-witness search (L62); SOS identity `Σqᵢ²−p≡0` verified symbolically (L80). | — |
| 26 | `mathmode/linear_algebra.py` | mathmode | disposer | CLEAN | `*_grade` grep hit (7 fns) | Exact-ℚ residual checks throughout: `A·x=b` (L73), `A·A⁻¹=I` (L89), Bareiss-vs-cofactor/-sympy dual determinant (L148-151), `A·v=λv` (L173), Sylvester residual (L277), invariant-factor similarity (L324), Jordan block-size sum (L365). | — |
| 27 | `mathmode/logic.py` | mathmode | disposer | CLEAN | `*_grade` grep hit (3 fns) | Z3 UNSAT cross-checked against exhaustive truth-table for small n (L76); SAT model independently re-substituted into φ (L110). | — |
| 28 | `mathmode/number_theory.py` | mathmode | disposer | CLEAN | `*_grade` grep hit (19 fns — the largest file) | Representative sample directly re-read by the auditor: `egcd_grade` (L36-45) verifies Bézout identity `a·x+b·y==gcd` AND divisibility AND cross-checks against `gcd()` independently (L40) before EXACT (L45), DECLINE otherwise (L42) — confirms the agent's evidence exactly. Same pattern repo-wide: modinv (L57 `a·a⁻¹≡1`), CRT (L78, all congruences), factorize (L265-271, product reconstructs n AND every factor independently primality-tested), Pell (L739 `h²−Nk²=1`), sieve (L851-852, MR + brute-force/π(n) dual check). | — |
| 29 | `mathmode/optimization.py` | mathmode | disposer | CLEAN | `*_grade` grep hit (1 fn) | LP `lp_max_grade`: three-part certificate — primal feasibility, dual feasibility, zero duality gap (L81-85) — ALL three required for EXACT. | — |
| 30 | `mathmode/probability.py` | mathmode | disposer | CLEAN | `*_grade` grep hit (3 fns) | Binomial: three exact identities (total=1, mean, variance, L44); Markov/Chebyshev are theorems (no per-input check needed, correctly EXACT-with-citation not a computed claim). | — |
| 31 | `mathmode/science_engineering.py` | mathmode | disposer | CLEAN | `*_grade` grep hit (2 fns) | Dimensional consistency: LHS/RHS exponent vectors must match exactly (L112) — an exact discrete check, not an approximation. | — |
| 32 | `mathmode/special_functions.py` | mathmode | disposer | CLEAN | `*_grade` grep hit (2 fns) | Gamma: functional equation + value identity both symbolically verified (L37); zeta-even: closed form cross-checked against the defining series partial sum (L56-59). | — |

### pillar3/ (19 files — CODE-mode transform/analysis passes; every disposer follows: real check → measured
whole-program win → grade, never a win-only or check-only shortcut)

| # | file | pkg | role | verdict | issue(class) | fix + regression | commit |
|---|------|-----|------|---------|--------------|------------------|--------|
| 33 | `pillar3/algorithms.py` | pillar3 | disposer | CLEAN | `*_grade` grep hit (`recognize_and_grade`, L37) | Differential check (L42) + metamorphic cross-check (L46-50) + measured win (L51) + magnitude bound (L52) all required before PROBABILISTIC (L61); DECLINE at L44,50,53-56 otherwise. | — |
| 34 | `pillar3/boundscheck.py` | pillar3 | disposer | CLEAN | `*_grade` grep hit (`guard_grade`, L38) | Z3 UNSAT proof of guard redundancy via fresh solver (L43, L26-34) + differential corroboration (L48) + measured win (L51) all gate EXACT. | — |
| 35 | `pillar3/bv_validate.py` | pillar3 | disposer | CLEAN | `*_grade` grep hit (`bv_grade`, L85) | Fresh `z3.Solver()` bitvector-equivalence check (L88, L29-41); only PROVEN → EXACT, REFUTED/UNKNOWN → DECLINE (L94,96). | — |
| 36 | `pillar3/convolution.py` | pillar3 | disposer | CLEAN | `*_grade` grep hit (`conv_grade`, L66) | Proven no-wraparound bound (L73) + full-vector spot-check against naive (L76-78) + measured win (L79) all required. | — |
| 37 | `pillar3/copyelim.py` | pillar3 | disposer | CLEAN | `*_grade` grep hit (`copyelim_grade`, L64) | AST mutation analysis (L68) + differential corroboration (L74) + measured win (L76) all required before EXACT (L83). | — |
| 38 | `pillar3/effects.py` | pillar3 | disposer | CLEAN | `*_grade` grep hit (`coalesce_grade`, L57) | Effect-window analysis (L63) + differential result check (L87-88) + measured win (L89) all required. | — |
| 39 | `pillar3/egraph_simplify.py` | pillar3 | disposer | CLEAN | `*_grade` grep hit (`egraph_grade`, L47) | Fresh-solver Z3 equivalence check (L55, L38-44) + measured win (L66) required before EXACT. | — |
| 40 | `pillar3/engine.py` | pillar3 | ADT-constructed (routing helper) | CLEAN | `*_grade` grep hit (`_decide_grade`, L130) | Not a raw-literal disposer — routes to `KV.EXACT`/`KV.DECLINE`/`KV.PROBABILISTIC` constructors only, based on `V.attempt_certificate()`'s outcome (L135); the ADT's own construction-time gates apply. | — |
| 41 | `pillar3/fixers/pipeline.py` | pillar3 | disposer | CLEAN | `*_grade` grep hit (`apply_and_grade`, L21) | Differential test (L27) + measured win (L34) always required; EXACT additionally requires a real `exact_justification` argument (L42), else PROBABILISTIC (L46) — never EXACT by default. | — |
| 42 | `pillar3/interproc.py` | pillar3 | disposer | CLEAN | `*_grade` grep hit (`memoize_grade_ip`, L39) | Interprocedural purity fixpoint (L44) + differential corroboration (L62-63) + measured win (L64) all required. | — |
| 43 | `pillar3/lifting.py` | pillar3 | disposer | CLEAN | `*_grade` grep hit (`lift_and_grade`, L97) | Differential oracle (L103) + Z3 dual proof of lift AND synthesis (L108-111) + measured whole-program win (L113-118) all required for EXACT (L121); PROBABILISTIC fallback if only the differential passes (L125-128). | — |
| 44 | `pillar3/matmul.py` | pillar3 | disposer | CLEAN | `*_grade` grep hit (`matmul_grade`, L52) | Proven overflow bound (L60) + full-matrix spot-check vs naive (L65) + measured win (L73-76) all required. | — |
| 45 | `pillar3/polysum.py` | pillar3 | disposer | CLEAN | `*_grade` grep hit (`polysum_grade`, L42) | k-induction proof of the closed form for ALL n (L47) + measured win (L52-53) required; DECLINE on proof failure (L49-50) or no win (L53-56). | — |
| 46 | `pillar3/purity.py` | pillar3 | disposer | CLEAN | `*_grade` grep hit (`memoize_grade`, L102) | Sound conservative AST purity proof (L106, `is_pure()` L32-99) + differential test (L124) + measured win (L126-127) all required. | — |
| 47 | `pillar3/round2.py` | pillar3 | disposer (9 fns) | CLEAN | `*_grade` grep hit (approx/bloom/native/cardinality/frequency/serialization/typespec/dead_branch/unswitch) | Each has its own real gate: empirical ε at 95th-percentile + measured win (approx L52-54, cardinality L310-311), invariant + rate checks (bloom L161/165, frequency L338/341), float-tolerant 8-case differential (native L251-257), round-trip check (serialization L404), monomorphism guard + differential (typespec L454/456), Z3 SAT/UNSAT (dead_branch L485-488), exhaustive flag-domain test (unswitch L537). All have DECLINE paths. | — |
| 48 | `pillar3/speculation.py` | pillar3 | disposer | CLEAN | `*_grade` grep hit (`speculation_grade`, L50) | Speculative-vs-on-demand equality check (L56) + measured misspeculation rate δ vs target (L60-62) required for PROBABILISTIC. | — |
| 49 | `pillar3/stoke.py` | pillar3 | disposer | CLEAN | `*_grade` grep hit (`stoke_grade`, L44) | Schwartz-Zippel equivalence verification (L49) + measured win (L61-62) required for PROBABILISTIC. | — |
| 50 | `pillar3/termination.py` | pillar3 | disposer | CLEAN | `*_grade` grep hit (`termination_grade`, L45) | Auditor-confirmed directly: `proves_termination()` (L28-42) runs TWO fresh `z3.Solver()` instances checking bounded-below (L32-33) and strictly-decreasing (L34-35) as UNSAT-of-negation goals; only both UNSAT ⇒ EXACT (L48-51), else honest DECLINE with the z3 counterexample (L52-53) — matches the agent's citation exactly. | — |
| 51 | `pillar3/transforms.py` | pillar3 | disposer | CLEAN | `*_grade` grep hit (`serialization_swap_grade`, L47) | Round-trip value-equivalence check across all workload items (L51) required before EXACT (L65-67); DECLINE on mismatch (L53). | — |

### catalog/ (32 files — mechanism/gap-fold engines from the §P/§AD/§CONSOL/§AK-AN campaigns; certificate =
z3 proof, exact re-substitution over ℚ, or cross-check against an independent oracle)

| # | file | pkg | role | verdict | issue(class) | fix + regression | commit |
|---|------|-----|------|---------|--------------|------------------|--------|
| 52 | `catalog/bitvector_ring.py` | catalog | disposer | CLEAN | `*_grade` grep hit (2 fns) | Z3 QF_BV proof (L75) + differential check at N=1000 on concrete values (L79-81) both required. | — |
| 53 | `catalog/blackbox_fallback.py` | catalog | disposer | CLEAN | `*_grade` grep hit (2 fns) | Berlekamp-Massey recovery on probe terms (L52) then EXACT prediction of HELD-OUT terms the recovery never saw (L60-71) — the divergence guard; DECLINE if the held-out prediction is wrong (L69-71). | — |
| 54 | `catalog/compose.py` | catalog | ADT enforcement | CLEAN | `*_grade` grep hit (`combine_grade`, L59) | This IS the weakest-link law itself (L61-68): EXACT∘EXACT→EXACT only, never an upgrade; `to_verdict()` (L260-261) raises on a detected false-upgrade — the ADT is the verification. | — |
| 55 | `catalog/decline_boundary.py` | catalog | disposer + guards | CLEAN | `*_grade` grep hit (`mdl_grade` + 3 guards) | `mdl_grade` (L91-103): real zlib-compression MDL test — compresses→EXACT (L101), doesn't→DECLINE (L102-103); the 3 guard functions are pattern-matching boundary CLASSIFIERS (Rice/incompressibility/turbulence), not live-input graders. | — |
| 56 | `catalog/detectors_b.py` | catalog | disposer | CLEAN | `*_grade` grep hit (2 fns) | Rank-revealing: exact RREF then each dependent row re-verified as an exact ℚ-combination (L62-68); poly-law: finite-difference degree fit then EVERY term re-verified via Newton-form regeneration (L106-107). | — |
| 57 | `catalog/distributed_state.py` | catalog | disposer | CLEAN | `*_grade` grep hit (`distributed_state_grade`, L126) | Affine extraction (L137-141) + z3 LIA dual proof — composed handlers ≡ sequential ∀s (L105-111) AND matrix-power ≡ N rounds for sampled N (L114-122) — both required (L147-148). | — |
| 58 | `catalog/equiv_check.py` | catalog | wrapper over verified result | CLEAN | `*_grade` grep hit (`equiv_grade`, L97) | Wraps an ALREADY-verified `EquivResult` from `prove_equiv_z3` (L32-54, fresh z3 UNSAT check) or `bounded_equiv` (L85-94, exhaustive); `equiv_grade` itself just reads `.proved` (L100) — the proof happens upstream, not skipped. | — |
| 59 | `catalog/gap_matrix.py` | catalog | disposer | CLEAN | `*_grade` grep hit (3 fns) | Kronecker: exact reconstruction `B⊗C==A` re-check (L72); block-low-rank: exact-ℚ rank computed per block (L103-115); router tries both, DECLINEs only if neither structure verifies (L142). | — |
| 60 | `catalog/gap_prob.py` | catalog | disposer | CLEAN | `*_grade` grep hit (2 fns) | Both graded PROBABILISTIC only (never EXACT, by design — §binding separation) and gated on a MEASURED (not guessed) relative error vs a stated ε (L35,111-113). | — |
| 61 | `catalog/gap_recur.py` | catalog | disposer | CLEAN | `*_grade` grep hit (3 fns) | All three dispose via exact-ℚ re-substitution over EVERY observed term/transition — nonlinear (L95), matrix `M·v[n-1]==v[n]` (L145-146), algebraic relation vanishes on every window (L184-186) — never a fit-only claim. | — |
| 62 | `catalog/gap_signal.py` | catalog | disposer | CLEAN | `*_grade` grep hit (3 fns) | Modulated: exact resynthesis check `a[i]==ρ^(i//P)·a[i%P]` for all i (L46); piecewise: both segments independently must return EXACT from the underlying engine (L68,71); sparse: k-sparsity bound checked in both bases (L134-141). | — |
| 63 | `catalog/gap_telescope.py` | catalog | disposer | CLEAN | `*_grade` grep hit (`zeilberger_grade`, L23) | The WZ identity is checked SYMBOLICALLY (`sp.simplify(...)!=0` must be False, L82) before the certificate is ever built (L84) — a real proof, not a guess from the Gosper/guesser step alone. | — |
| 64 | `catalog/holonomic_sum.py` | catalog | disposer (thin router) | CLEAN | `*_grade` grep hit (`holonomic_sum_grade`, L32) | Routes to `gap_telescope.zeilberger_grade` (L61) and only accepts its EXACT verdict (L62) — inherits row 63's real proof, doesn't bypass it. | — |
| 65 | `catalog/lazy_decline.py` | catalog | disposer | CLEAN | `*_grade` grep hit (2 fns) | Telescoping identity `G(k+1)-G(k)≡body` checked symbolically (L60-62); DECLINE cascade is exhaustive (parse/loop/Gosper/identity/black-box, L33-108) before ever returning DECLINE outright. | — |
| 66 | `catalog/lift.py` | catalog | disposer | CLEAN | `*_grade` grep hit (3 fns) | Z3 induction proof — base case AND inductive step both required (`inductive_sum_equiv`, L101, verified against the equiv_check internals L67-77) — a textbook induction proof, not a pattern match. | — |
| 67 | `catalog/mech_aara.py` | catalog | disposer | CLEAN | `*_grade` grep hit (`aara_grade`, L119) | Z3 feasibility solve (L127-132) THEN an independent `_reverify()` that re-runs fresh z3 solvers on every amortized-cost property from scratch (L134-135, L83-116) — a genuine double-check, not one solve trusted twice. | — |
| 68 | `catalog/mech_aperiodic.py` | catalog | disposer | CLEAN | `*_grade` grep hit (`aperiodic_grade`, L46) | Exact combinatorial tests — non-periodicity (L64) and per-factor balance (L66) — both computed directly, not estimated. | — |
| 69 | `catalog/mech_causal.py` | catalog | disposer | CLEAN | `*_grade` grep hit (`causal_grade`, L97) | Exact d-separation via the moralized-ancestral-graph algorithm (L92-94) backs the back-door adjustment search (L113); DECLINE when no valid adjustment set exists (L114-117). | — |
| 70 | `catalog/mech_conley.py` | catalog | disposer | CLEAN | `*_grade` grep hit (`conley_grade`, L83) | Exact cubical relative homology over 𝔽₂ via boundary-matrix rank reduction (L93-94, L40-60) — a real algebraic computation, not a heuristic index guess. | — |
| 71 | `catalog/mech_defective.py` | catalog | disposer | CLEAN | `*_grade` grep hit (`defective_grade`, L121) | Monomial-basis closure check (L133-138) THEN exact ℚ forward-loop evaluation with term-by-term re-substitution (`if Mn[tgt_idx]!=actual`, L156) — the linearization is re-verified, not assumed correct because it closed. | — |
| 72 | `catalog/mech_edgecover.py` | catalog | disposer | CLEAN | `*_grade` grep hit (`edgecover_grade`, L62) | LP solve via z3.Optimize (L71-74) THEN an explicit exact-ℚ feasibility re-check that every attribute is actually covered (L77-79) — the solver's answer is independently re-verified. | — |
| 73 | `catalog/mech_flow.py` | catalog | disposer | CLEAN | `*_grade` grep hit (`flow_grade`, L57) | Exact ℚ Laplacian/component count (L67-68) + 60-step forward run with STRICT Dirichlet-energy-decrease monotonicity checked every step (L74-85) — a real dynamical proof, not a one-shot check. | — |
| 74 | `catalog/mech_knot.py` | catalog | disposer | CLEAN | `*_grade` grep hit (`knot_grade`, L87) | Full 2ⁿ Kauffman-bracket state-sum expansion (L97, L52-73) with writhe normalization (L99, L76-84); Reidemeister invariance is a mathematical property of the skein relation itself (documented at L100-102), not an unverified claim; DECLINE above 14 crossings citing #P-hardness (L94-96) rather than silently truncating. | — |
| 75 | `catalog/mech_kregular.py` | catalog | disposer | CLEAN | `*_grade` grep hit (`kregular_grade`, L152) | Auditor-confirmed directly: L171 `all(eval_representation(A,v,w,n,k)==a[n] for n in range(N))` re-derives EVERY term of the sequence from the fitted k-kernel representation before accepting it, plus a compression-ratio guard (L180-182) and a z3 spot-check (L184) — matches the agent's citation exactly; this is the flagship "brand-new mechanism" from §P1f, and its own EXACT gate is real. | — |
| 76 | `catalog/mech_mpst.py` | catalog | disposer | CLEAN | `*_grade` grep hit (`mpst_grade`, L124) | Syntactic per-role projection with exception on failure (L133-135) THEN synchronous-product BFS reachability safety check (L139, L92-121) — both required. | — |
| 77 | `catalog/mech_persistence.py` | catalog | disposer | CLEAN | `*_grade` grep hit (`persistence_grade`, L107) | Exact 𝔽₂ boundary-matrix barcode (L117, L45-84) + a genuine stability WITNESS: points perturbed by r, barcode recomputed, bottleneck distance re-checked (L124-138) — not just a one-shot barcode claim. | — |
| 78 | `catalog/mech_seminewton.py` | catalog | disposer | CLEAN | `*_grade` grep hit (`seminewton_grade`, L164) | TWO independent fixpoint algorithms (Kleene + Newton, L172-173) cross-checked against each other (L180) AND re-substituted into the original system (L178) — double independent verification. | — |
| 79 | `catalog/mech_sfa.py` | catalog | disposer | CLEAN | `*_grade` grep hit (`sfa_grade`, L104) | Theory-decidability check (L111-113) gates a z3-backed symbolic bisimulation equivalence proof (L114, L59-101) — DECLINEs honestly outside the decidable fragment rather than guessing. | — |
| 80 | `catalog/mech_sheaf.py` | catalog | disposer | CLEAN | `*_grade` grep hit (`sheaf_grade`, L49) | Exact-ℚ coboundary rank via sympy (L62-65); section-gluing check `δ⁰s=0` (L74) or global-section existence via exact kernel dimension (L84-89) — both are real linear-algebra computations. | — |
| 81 | `catalog/mech_tev.py` | catalog | disposer | CLEAN | `*_grade` grep hit (`tev_grade`, L89) | Exact-ℚ re-substitution check across EVERY sequence term (L102) plus, for the polynomial case, a z3 ∀i proof (L106, L133-180) — re-verifies, doesn't just trust the CR-fit. | — |
| 82 | `catalog/mobius_fold.py` | catalog | disposer | CLEAN | `*_grade` grep hit (2 fns) | Z3 NRA cleared-denominator polynomial-identity proof for sampled N (L99, L36-87) — a real algebraic identity check, not a numeric coincidence; degenerate (ad−bc=0) and degree≥2 cases explicitly DECLINEd (L98,124). | — |
| 83 | `catalog/topic_a.py` | catalog | disposer (router) | CLEAN | `*_grade` grep hit (4 fns) | Routes to equality-saturation (L25), z3 translation-validation (L41), and certified-extraction-with-verified-flag (L54-59) — each sub-engine's own real check gates EXACT; DECLINE on unsound/no-improvement (L33-34,47,65). | — |

### root / gpu / native / newengine / metakernel / qfold / extract (26 files — decision-procedure and structure-
recognition engines, mostly from the "native" dependency-free-reimplementation campaign)

| # | file | pkg | role | verdict | issue(class) | fix + regression | commit |
|---|------|-----|------|---------|--------------|------------------|--------|
| 84 | `autodiff.py` | (root) | disposer | CLEAN | `*_grade` grep hit (`autodiff_grade`, L126) | Dual-number forward-mode result cross-checked against independent sympy symbolic differentiation for EVERY partial (L141-148) — two different algorithms must agree exactly. | — |
| 85 | `cp_decompose.py` | (root) | disposer | CLEAN | `*_grade` grep hit (`cp_decompose_grade`, L53) | Exact entry-by-entry tensor re-composition check `_outer(a,b,c)==T` (L70); rank>1 (NP-hard general case) honestly DECLINEd rather than guessed (L65-67). | — |
| 86 | `extract/tensor_contract.py` | extract | disposer | CLEAN | `*_grade` grep hit (`contract_grade`, L95) | Variable-elimination result cross-checked against an independent naive full-sum computation (L112-115); treewidth-blowup and float-input cases honestly DECLINEd (L103-104,109-111). | — |
| 87 | `gpu/hidden_structure.py` | gpu | disposer (4 fns) | CLEAN | `*_grade` grep hit | Every structure claim is an EXACT entry-by-entry re-check: low-rank factorization residual (L69), circulant N² entries (L99-100), Toeplitz diagonal constancy (L119-120), Kronecker (p·q)² entries (L151-152) — never inferred from a partial sample. | — |
| 88 | `gpu/ptx_codegen.py` | gpu | disposer | CLEAN | `*_grade` grep hit (`kernel_grade`, L149) | Auditor-confirmed directly: `translation_validate()` (L117-138) runs the kernel against a REFERENCE GEMM on a battery including the ragged-K remainder case, requires bit-exact equality (`got != ref` → DECLINE, L129-131) — never trusts a kernel that hasn't been checked against ground truth; matches the agent's citation exactly. | — |
| 89 | `groebner.py` | (root) | disposer | CLEAN | `*_grade` grep hit (2 fns) | Cofactor witness independently re-checked by polynomial expansion to zero (L90); NO-case backed by Buchberger S-pair re-verification on all basis pairs (L100-108); EXPSPACE step-cap honestly DECLINEd, not silently truncated (L58-60). | — |
| 90 | `guaranteed_structure.py` | (root) | disposer (4 fns) | CLEAN | `*_grade` grep hit | Erdős–Szekeres run re-checked for actual monotonicity+length (L59-61); pigeonhole state equality directly re-verified (L74-75); Ramsey triangle colours directly re-checked (L94) — all Ramsey-theory GUARANTEES are re-derived on the actual instance, not assumed from the theorem alone. | — |
| 91 | `hermite.py` | (root) | disposer | CLEAN | `*_grade` grep hit (`hermite_reduce_grade`, L51) | Certificate `(rational)′+reduced≡integrand` verified by symbolic differentiation to zero (L62), plus a squarefree-denominator precondition check (L66). | — |
| 92 | `metakernel/chc_kernel_bridge.py` | metakernel | disposer (kernel-attribution router) | CLEAN | `*_grade` grep hit (2 fns) | Three Horn VCs independently re-derived and confirmed by a ZERO-z3 propositional/ground-EUF DPLL kernel (L189-191, `metakernel.trusted_kernel`); anything outside the decidable EUF fragment DEFERS to the unmodified `chc_solve.chc_grade()` (L218-241) rather than inventing a verdict — a genuinely more conservative TCB-reduction layer, not a bypass. | — |
| 93 | `native_lattice.py` | (root) | disposer (4 fns) | CLEAN | `*_grade` grep hit | LLL: unimodular-transform residual check `U·B==R` (L74); integer-relation: full-precision re-check + spurious-candidate rejection (L101-106); Diophantine: particular solution substituted back exactly (L191-192). | — |
| 94 | `native_modelcount.py` | (root) | disposer | CLEAN | `*_grade` grep hit (`model_count_grade`, L84) | Auditor-confirmed directly: TWO independent DPLL variable-orderings must agree (L90-95) AND, for ≤20 variables, a THIRD independent brute-force oracle must also agree (L96-98) — three-way cross-check before EXACT; matches the agent's citation exactly. | — |
| 95 | `native_prng.py` | (root) | disposer (2 fns) | CLEAN | `*_grade` grep hit | LCG/LFSR recovery is verified by predicting HELD-OUT stream output the recovery algorithm never saw (L59-60, L84) — the same held-out-divergence-guard pattern as `blackbox_fallback.py`, applied to PRNG state recovery. | — |
| 96 | `native_realroots.py` | (root) | disposer | CLEAN | `*_grade` grep hit (`realroots_grade`, L108) | Isolated root count cross-checked against an independent Sturm sign-variation count (L118-121) — two different counting methods must agree. | — |
| 97 | `native_rewrite.py` | (root) | disposer (2 fns) | CLEAN | `*_grade` grep hit | Knuth-Bendix completion: EVERY critical pair re-verified to actually join, confluence is re-checked not assumed from convergence alone (L107-108). | — |
| 98 | `native_sequence.py` | (root) | disposer (2 fns) | CLEAN | `*_grade` grep hit | Berlekamp-Massey: recurrence run-forward-verified on held-out terms (L68-69, `_verify_recurrence` L50-55); grammar repair: lossless round-trip re-expansion check (L183-184). | — |
| 99 | `native_telescope.py` | (root) | disposer | CLEAN | `*_grade` grep hit (`telescope_grade`, L60) | Antidifference certificate `S(n+1)-S(n)-t(n)` symbolically simplified to exactly 0 (L76); DECLINE if the check is nonzero (L77-79) rather than trusting the Gosper step alone. | — |
| 100 | `native_unify.py` | (root) | disposer | CLEAN | `*_grade` grep hit (`unify_grade`, L66) | MGU re-checked by APPLYING it to both sides and confirming equality (L72) — the substitution is verified against the actual terms, not trusted from the algorithm's internal state. | — |
| 101 | `newengine3/csp_dichotomy.py` | newengine3 | disposer | CLEAN | `*_grade` grep hit (2 fns) | Six polymorphism closures tested exhaustively over every relation tuple (L91-96); NP-complete classification backed by explicit, re-checkable refuting witnesses (L109-113); undecidable PCSP fragment explicitly DECLINEd (L121-123). | — |
| 102 | `newengine5/morse_inequalities.py` | newengine5 | disposer | CLEAN | `*_grade` grep hit (2 fns) | Weak+strong Morse inequalities AND the Morse-Euler equation all re-checked exactly; any single violation → DECLINE (L45-46,52-53,58). | — |
| 103 | `newengine5/tree_automata.py` | newengine5 | disposer | CLEAN | `*_grade` grep hit (3 fns) | Membership re-run bottom-up independently of however the input was constructed (L103); emptiness witness term is re-run through membership as its own proof (L135-136); constrained/undecidable automata fragments explicitly DECLINEd (L98-100,115-117). | — |
| 104 | `newton_series.py` | (root) | disposer (3 fns) | CLEAN | `*_grade` grep hit | Bostan-Mori: independent term-extraction cross-check (L164-173); multipoint eval: subproduct-tree result compared against direct Horner evaluation on every point (L241-243); Newton-series identities verified to order n (L268-274). | — |
| 105 | `qfold/displacement.py` | qfold | disposer | CLEAN | `*_grade` grep hit (`displacement_grade`, L62) | Named structures (Toeplitz/Hankel/Vandermonde/Cauchy) verified by exhaustive entry-by-entry checks (L24-51,80-88); low-rank case verified via exact rank factorization + generator-residual re-check (L90-103); float input honestly DECLINEd as unstable rather than approximated (L68). | — |
| 106 | `renormalize.py` | (root) | disposer (2 fns) | CLEAN | `*_grade` grep hit | Markov lumpability condition re-checked (uniform block-transitions, L37-40,69-71) with an exact-ℚ stationary distribution via Gaussian elimination (L74); multigrid residual-tolerance DECLINE path (L102-104). | — |
| 107 | `sos_cert.py` | (root) | disposer | CLEAN | `*_grade` grep hit (2 fns) | Inertia signs determined exactly via Sturm on the characteristic polynomial (L172-174); SOS identity `zᵀQz≡p` re-verified exactly AND PSD-ness confirmed via zero-negative-eigenvalue Sturm check (L118-119,204) — no SDP solver dependency, no unverified claim. | — |
| 108 | `string_solver.py` | (root) | disposer | CLEAN | `*_grade` grep hit (`string_grade`, L119) | Z3 SAT model independently re-substituted into EVERY constraint before being trusted (L124-125, `_recheck` L97-116) — the solver's own answer is not taken on faith. | — |
| 109 | `zx_normalize.py` | (root) | disposer | CLEAN | `*_grade` grep hit (`zx_grade`, L68) | Normalized ZX-circuit re-run through `verify_equality()` against the original via exact tensor comparison (L59,63-64,74-77); circuits too large to verify exactly honestly DECLINE rather than trusting pyzx's simplification unchecked (L42-47). | — |

*(Batch 1 is now COMPLETE: all 109 files with a named root disposer or a `*_grade` function have rows above —
16 originally + 93 in this pass, all CLEAN except row 7 (`recall_integrate.py`, FLAGGED, unresolved pending
owner direction, zero measured live callers). Resume point for the next session: Batches 2–6, per the
directive's own risk-ordered package list [`mathmode/`·`pillar3/`·`catalog/` are now covered ABOVE this line,
ahead of schedule — remaining: `newengine*`/`qmkernel`/`metakernel`/`qfold`/`conjecture`/`barrierfold`(files
without a `*_grade` function, e.g. registries/reports/routers not yet individually walked)·`extract`/`recall`/
`accel`/`mechanisms`/`security`/`frontend`/`gapfold`·`webapi`/server/provider/UI·remaining root+docs]. Those
batches' files that DO contain a `*_grade` function are now already covered above; what remains for Batches 2-6
is the NON-`*_grade` remainder of those packages — reports, registries, routers, UI, and files whose grade
comes from a mechanism that lives in an already-audited file's call chain.)*
