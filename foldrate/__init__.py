"""
§AA — FIVE FOLD-RATE WEAPONS: not new structure to recognize, but better EXTRACTION of structure already there.
================================================================================================================
Fold rate = (recognizable structure) × (ability to surface it). Every prior directive grew the first factor toward its
~15% ceiling; these five grow the SECOND. ★ All five are LLM-FREE, z3-verified, deterministic — they touch the
compiler's structural machinery, not the proposer's intelligence, so they work identically with a weak LLM (the binding
design constraint: the product must support weak LLMs, so an LLM-proposer would only slow things and degrade results).
  • WEAPON 1 — Canonicalization (the MULTIPLIER, built first): the same foldable structure is written many ways; a
    semantics-preserving normal form (z3-proved) before fold makes EVERY lens/mechanism catch more at once. Measured
    BEFORE/AFTER on the same corpus — a multiplier across all detectors, not an addition to one.
  • WEAPON 2 — Lens Composition: chain lenses so one transform exposes structure another folds. Additive-with-overlap,
    NEVER multiplicative — the real composed lift measured, overlap subtracted (no "30–50%" overclaim).
  • WEAPON 3 — Speculative/Conditional Fold (full §X-P1): guard the one dynamic parameter, check it at runtime, dual-
    path. Runtime information (not the LLM) is the observer — the honest Maxwell's-demon. Structured inputs only.
  • WEAPON 4 — Memoization Cache Extension (§V): the same fold proved once, then O(1) lookup. Raises VALUE not rate;
    cold-vs-warm separated; sound keys (a wrong hit FAILS the build).
  • WEAPON 5 — Domain-Idiom Library: register recurring idioms of numeric/signal/stats/crypto/ML-preproc. ★ Corpus
    honesty — a signal idiom lifts the SIGNAL fold rate, never the backend 5.7% (the corpus-swap trick, forbidden).

★ INHERITED HONESTY SPINE: fold rate = APPLIED not issued; fold-rate ≠ speedup; precision 1.0 (every rewrite/guard/
key/idiom z3-proved or proved-once); the multiplier measured before/after; composition not overclaimed; cold-vs-warm
for the cache; domain-vs-backend for idioms; LLM-free stated; NO new certificate kind (feeds existing machinery);
zero-dep (`forbidden_present == []`); the pigeonhole wall stands (none folds random). Never imported by test_build.
Modules: canonicalize · compose · speculative · foldcache · domain_idioms · foldrate_report.
"""
