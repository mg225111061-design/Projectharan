"""
§AY — QUANTUM LINEAR-STRUCTURE FOLD (12 recognition branches; NOT a new mechanism — 14/22 saturation UNCHANGED).
================================================================================================================
Every module here is a NEW RECOGNITION BRANCH of the EXISTING engine (a wider proposer aperture), reusing the
repo primitives (cfinite.companion_nth, native_sequence.berlekamp_massey_Q / gf2_solve, gpu.hidden_structure.
exact_rank_factorization, foldaxes.probabilistic_fold, catalog.equiv_check). The GLM insight: saturation is a
proposer limit, not a z3 limit — so these are extra angles for the SAME verifier, never a 23rd mechanism.

★ VERIFIER TRUTH (§0): a ∀-n fold is NOT z3 array-induction (prove_exact says that is out of scope). ∀-n comes
ONLY from (a) a telescoping/step identity over FINITE variables (z3 QF_LRA/QF_NRA, decidable), or (b) a STRUCTURE
THEOREM (companion-matrix / minimal-polynomial / Cayley–Hamilton, ∀-n by construction) gated by exact held-out
replay. No cert here claims "z3 proves ∀-n".

★ EXACT BOUNDARY (§1.8): EXACT only inside a structure class — commuting / finite-invariant-subspace / low-rank /
Clifford / Gaussian. Non-commuting / infinite-invariant / full-rank / non-Clifford / chaotic / FLOAT ⇒ DECLINE.
Outside the class an EXACT verdict would be a false-EXACT = constitutional violation.

★ Axis A (recognition/coverage) and Axis B (speedup) are NEVER summed. B is asserted only after crossover.
★ claims of "quantum-origin speedup" are permanently banned (no quantum HW ⇒ only classical linear-structure theorems
  cross) — the banned bigram is never written contiguously, even here.
"""
