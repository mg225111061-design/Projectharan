# HARAN web backend runtime deps. (Coq, if wanted for unbounded-∀ proofs, is a system package — see
# Dockerfile INSTALL_COQ; it is NOT a pip dep.) The Claude API key is NEVER a dependency or env var —
# it is entered per request in the UI (level-1).
fastapi>=0.110
uvicorn[standard]>=0.27
anthropic>=0.40
openai>=1.40          # only used when HARAN_PROVIDER=openai_compat (OpenRouter/TokenMix-style gateways)
sympy>=1.12
z3-solver>=4.12
# Optional (v28 S20): an error-recovering, comment/string-correct multi-language frontend. If these are
# absent, the pure-Python fallback in treesitter_frontend.py carries the SAME soundness fix — correctness
# never depends on them. Install to enable the real Tree-sitter CST path (add grammars as needed):
#   pip install tree_sitter tree_sitter_go        # (tree_sitter_c, tree_sitter_rust, … per language)
tree_sitter>=0.23        # optional
tree_sitter_go>=0.23     # optional (Go grammar; one grammar package per language)
# Optional (v28 S22): multi-format file ingestion. stdlib formats (json/ipynb/csv/text/zip/tar) need
# nothing; these enable office/PDF/OCR. file_ingest.py degrades honestly (BLOCKED) if any are absent —
# correctness/labels never depend on them. (OCR also needs the system `tesseract` binary.)
python-docx>=1.1         # optional (.docx)
openpyxl>=3.1            # optional (.xlsx)
pypdf>=4.0               # optional (.pdf; needs a working `cryptography` backend)
# pytesseract + Pillow + tesseract(system) — optional, for image OCR
