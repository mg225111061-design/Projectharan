"""
SOUL-DEEP OPTIMIZATION (§M MOVE 3) — the verified A/B/C/D engine driven to each domain's provable limit.
=====================================================================================================
Not "transcend by computation" — exhaust every PROVED-safe optimization on the hot paths systems and mobile
ACTUALLY have: locks, allocation, syscalls, data structures (systems); network round-trips, render recomputation,
serialization, battery (mobile). Each optimization proved semantics-preserving before applied; each domain driven
to a measured limit and no further, the residual named honestly (irreducible physical latency / already-optimal).
Reuses the accel/ A/B/C/D modules; zero new deps; never imported by test_build.
"""
